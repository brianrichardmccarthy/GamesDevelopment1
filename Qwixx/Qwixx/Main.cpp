#include "Board\BoardHeader.h"

int main(void) {

    /* board::die::Die die("red");
    
    for (int x = 0; x < 100; x++) {
        die.roll();
        std::cout << die.toString() << std::endl;
    } */

    system("PAUSE");
    return 0;
}